export default {
  content: ["./index.html", "./src/**/*.{js,jsx}"],
  theme: {
    extend: {
      colors: {
        primary: "#6B21A8",   // morado profundo
        gold: "#D4AF37",      // dorado elegante
        soft: "#F5F3FF",      // fondo suave
        dark: "#1E1B4B"       // oscuro elegante
      }
    }
  },
  plugins: [],
}